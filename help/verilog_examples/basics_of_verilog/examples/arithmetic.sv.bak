//examples of arithmetic

//here i will demonstrate arithmetic capabilities in verilog while also making a component

module ALU (input[7:0] A, input[7:0] B, input[3:0] op, output[15:0] arithmeticoutput);

if(op == 0)
//we can do simple binary addition
	arithmeticoutput = A + B;
else if(op == 1)
//we can do simple subtraction
	arithmeticoutput = A - B;

endmodule;